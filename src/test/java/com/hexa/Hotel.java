package com.hexa;

import java.text.DateFormat;
import java.text.SimpleDateFormat;
import java.util.concurrent.TimeUnit;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.support.ui.Select;

public class Hotel {
	public static void main(String[] args)  {
		/*BaseClass  b = new BaseClass();
		WebDriver driver = b.launchBrowser();
		b.launchUrl("https://adactinhotelapp.com/");
	LoginPojo lp = new LoginPojo();
		
		
		
		b.type(lp.getTxtUser(),"kaushiks24");
		b.type(lp.getTxtPass(),"Kaushiks24");
		b.btnClick(lp.getBtnLogin());
		Location lo =new Location();
		b.type(lo.getLocation(),"London");
		b.type(lo.getHotel(), "Hotel Hervey");
		b.type(lo.getRoom(), "Double");
		b.type(lo.getRoomno(), "2");
		b.type(lo.getDatepck(), "23/09/2021");
		b.type(lo.getDateout(), "25/09/2021");
	    b.type(lo.getAdult(), "2");
	    b.type(lo.getChild(), "1");
		b.btnClick(lo.getSearch());
		Radio ra = new Radio();
		b.btnClick(ra.getRadiobtn());
		b.btnClick(ra.getContin());
		Card ca = new Card();
		b.type(ca.getFirstname(), "kaushik");
		b.type(ca.getLastname(), "s");
		b.type(ca.getAddr(), "chennai 2nd street");
		b.type(ca.getCreditcard(), "1234567891234567");
		b.type(ca.getCardtype(), "MAST");
		b.type(ca.getMonth(), "January");
		b.type(ca.getYear(), "2022");
		b.type(ca.getCvv(), "321");
		b.btnClick(ca.getBooknow());
			
		
		Order o = new Order();
		driver.manage().timeouts().implicitlyWait(100,TimeUnit.SECONDS);
		b.getText(o.getOrderno());*/
			
		
		
	}

}
